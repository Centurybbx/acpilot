{
  "name": "acpilot",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.6.4",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:run": "pnpm -r test:run"
  }
}
